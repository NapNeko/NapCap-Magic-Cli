# 字节编译 / 优化 / 动态链接库文件
__pycache__/
*.py[cod]
*$py.class

# C 扩展
*.so

# 分发 / 打包
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  通常这些文件由模板中的Python脚本生成
#  然后在PyInstaller构建exe之前写入，以便注入日期/其他信息。
*.manifest
*.spec

# 安装日志
pip-log.txt
pip-delete-this-directory.txt

# 单元测试 / 覆盖率报告
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# 翻译
*.mo
*.pot

# Django 文件
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask 文件
instance/
.webassets-cache

# Scrapy 文件
.scrapy

# Sphinx 文档
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   对于库或包，可能希望忽略这些文件，因为代码意图在多个环境中运行；否则，将它们检入：
# .python-version

# pipenv
#   根据 pypa/pipenv#598，建议在版本控制中包含 Pipfile.lock。
#   但是，如果是协作项目，若存在特定平台的依赖或没有跨平台支持的依赖，
#   pipenv 可能会安装无法工作的依赖，或没有安装所有需要的依赖。
#Pipfile.lock

# poetry
#   类似于 Pipfile.lock，一般建议在版本控制中包含 poetry.lock。
#   尤其对于二进制包，这确保了可重现性，而对于库则更常被忽略。
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   类似于 Pipfile.lock，一般建议在版本控制中包含 pdm.lock。
#pdm.lock
#   pdm 将项目范围的配置存储在 .pdm.toml，但建议不将其包括在版本控制中。
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; 被例如 github.com/David-OConnor/pyflow 和 github.com/pdm-project/pdm 使用
__pypackages__/

# Celery 文件
celerybeat-schedule
celerybeat.pid

# SageMath 解析文件
*.sage.py

# 环境
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder 项目设置
.spyderproject
.spyproject

# Rope 项目设置
.ropeproject

# mkdocs 文档
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre 类型检查器
.pyre/

# pytype 静态类型分析器
.pytype/

# Cython 调试符号
cython_debug/

# IDEA
.vscode/
.idea/

# PyCharm
#  JetBrains 的特定模板维护在一个单独的 JetBrains.gitignore 中，可以
#  在 https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore 找到
#  并可以添加到全局 gitignore 或合并到此文件。对于更彻底的选项（不推荐），可以取消注释以下内容以忽略整个 idea 文件夹。
#.idea/
